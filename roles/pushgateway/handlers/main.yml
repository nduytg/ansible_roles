---
- name: restart pushgateway
  become: true
  ansible.builtin.systemd:
    daemon_reload: true
    name: pushgateway
    state: restarted
    enabled: yes

- name: reload pushgateway
  become: true
  ansible.builtin.systemd:
    name: pushgateway
    state: reloaded
