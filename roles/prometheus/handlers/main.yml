---
- name: restart prometheus
  become: true
  ansible.builtin.systemd:
    daemon_reload: true
    name: prometheus
    state: restarted
    enabled: yes

- name: reload prometheus
  become: true
  ansible.builtin.systemd:
    name: prometheus
    state: reloaded
