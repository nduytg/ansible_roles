---
# handlers file for nginx
- name: restart nginx
  ansible.builtin.systemd:
    name: nginx
    daemon_reload: true
    state: restarted
    enabled: yes
    
- name: reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
